snapshots:
  - name: scd_dim_hosts
    relation: ref('dim_hosts')
    config:
      database: AIRBNB
      schema: snapshots
      alias: scd_dim_hosts
      unique_key: HOST_ID
      strategy: timestamp
      updated_at: HOST_CREATED_AT
      dbt_valid_to_current: "to_date('9999-12-31')"